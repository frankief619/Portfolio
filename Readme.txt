Overview

This portfolio website is a fully responsive, interactive site built to display my projects and skills. It’s hosted on GitHub Pages for easy accessibility and is designed with clean, modular code for maintainability.

Features

Responsive Design: Adapts to any screen size, from mobile to desktop.
API Integration: Displays dynamic content from third-party APIs.
Interactive Elements: Includes interactive components powered by JavaScript.
Hosted on GitHub Pages: Continuous deployment ensures that the latest changes are always live.
Technologies Used

HTML5 and CSS3
JavaScript
Bootstrap for responsive design
Git/GitHub for version control and hosting

Installation

To set up the project locally, follow these steps:

Prerequisites
Make sure you have the following installed:

Node.js (for package management if you plan to add tooling)
Git (for cloning the repository)
Clone the Repository

Open your terminal.
Clone the repository using the following command:
bash

Copy code
git clone https://github.com/frankief619/Portfolio.git
Navigate to the project directory:
bash
Copy code
cd Portfolio


Usage

Running the Project Locally
To view the website locally, you can simply open the index.html file in your browser.

Open the project directory in your terminal.
Start a local server (if you want to view it on localhost instead of opening the file directly):
Using VS Code:
Install the Live Server extension.
Right-click index.html and select Open with Live Server.

